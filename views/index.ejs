<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %></p>
    <h4>Instructions</h4>
    <p>1. Download data from ticket2u.com or justrunlah</p>
    <p>
      2. Delete the birthdate column and save the excel file ( for ticket2u )
    </p>
    <p>3. Select data source options and give the name for the file</p>
    <p>4. Upload data by clicking the upload a file button below</p>
    <p>5. Data will be automatically downloaded.</p>

    <!--  SINGLE FILE -->
    <form
      name="myForm"
      action="/upload"
      enctype="multipart/form-data"
      method="POST"
      onsubmit="return validateForm()"
    >
      <label for="">File name</label>
      <br />
      <input type="text" name="filename" placeholder="input file name" />
      <br /><br />
      <label for=""> Data source</label>
      <br />
      <select name="source" id="">
        <option value="justrun">JustRunLah</option>
        <option value="ticket2u">Ticket2U</option>
      </select>
      <br /><br />
      <label for="">Select file</label>
      <br />
      <input type="file" name="myFile" />
      <br />
      <br />
      <input
        style="height:30px; width:50px;background-color:#990102; color:white;"
        type="submit"
        value="Submit"
      />
    </form>
  </body>
  <script>
    function validateForm() {
      var x = document.forms["myForm"]["filename"].value;
      if (x == "") {
        alert("File name must be filled out");
        return false;
      }
      var x = document.forms["myForm"]["myFile"].value;
      if (x == "") {
        alert("File must be selected");
        return false;
      }
    }
  </script>
</html>
